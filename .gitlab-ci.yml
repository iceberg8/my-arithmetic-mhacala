stages:
  - test

image: python:3.12

before_script:
  - echo "Setting up environment"
  - pip install --upgrade pip
  - pip install hatch

test_job:
  stage: test
  script:
    - echo "Running tests"
    - hatch test-c

  coverage: '/\s+(\d+%)$/'